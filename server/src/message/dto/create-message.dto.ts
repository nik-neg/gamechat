import { User } from 'src/user/entities/user.entity';

export class CreateMessageDto {
  id: number;
  content: string;
  // isQuestion: boolean;
  // likes: number;
  // createdAt: Date;
  // updatedAt: Date;
  // user: User;
}
